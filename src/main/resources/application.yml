
spring:
  application:
    name: contact-service

  h2:
    console:
      enabled: true

server:
  port: 9090

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost